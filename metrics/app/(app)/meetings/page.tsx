// Meetings Tracker page — replaced with the ported component in a
// subsequent commit.
export default function MeetingsPage() {
  return (
    <div className="rounded border border-slate-200 bg-white p-6">
      <h1 className="text-lg font-semibold">Meetings Tracker</h1>
      <p className="mt-2 text-sm text-slate-500">
        Ported view coming in the next commit.
      </p>
    </div>
  );
}
